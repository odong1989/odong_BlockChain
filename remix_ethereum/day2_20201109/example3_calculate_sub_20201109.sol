pragma solidity >=0.4.22 <0.7.0;

contract example3_20201109 { 
    
    // 앞의 괄호에는 input값이 뒤의 괄호에는 output 값
    function sub(uint d, uint e) public view returns (uint256) {
        return (d-e);
    }
}
